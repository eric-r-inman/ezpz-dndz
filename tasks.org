#+title:     Tasks
#+language:  en

The project's backlog.  Record plans and work items here as ~* TODO~ entries —
see the "Capturing plans" guidance in =llms.org= — cross-linking related entries
and marking finished work ~* DONE~.

* DONE Relational storage migration (SQLite + Postgres, no JSON stores)

Replace every =JsonFileStore=-backed store with a fully relational schema served
by sqlx over two interchangeable backends: SQLite (default, at
=<data_dir>/ezpz-dndz.db= — zero-ops dev and small deploys) and PostgreSQL
(=--database-url postgres://…= for hosted scale to hundreds of users).  No
document stores: no JSON/JSONB columns anywhere; the Elm wire formats are
normalized into real tables, with the server-side codecs mirroring the Elm
=*.Wire= modules (including their legacy-shape leniency, which the one-shot
import needs).

Constraints: the HTTP API shapes stay byte-compatible (the Elm frontend is
untouched; existing integration tests pass unmodified); storage primitives live
in the lib crate; semantic thiserror errors; portable DDL (TEXT ids, INTEGER
timestamps, =$n= placeholders, which SQLite also accepts).  On first boot
against an empty database the server imports the legacy JSON files from the data
dir one-shot (marker table suppresses re-runs); the JSON files are left in place
as the rollback copy.

Phases: (1) db core + users + dice history, (2) the five per-user preset stores,
(3) compendium (user creatures, groups, saves), (4) encounters (live
+ named saves, shared schema keyed by slot), (5) retire JsonFileStore + CLI port
  + backup script + docs, (6) live Postgres verification.

Accepted tradeoff, noted for the record: the server previously round-tripped
most payloads as opaque JSON so the frontend owned those schemas outright;
normalization means future Elm wire changes to persisted shapes need a matching
server migration.

* DONE Finish the compliance sync against rust-template v0.135.0

The structural checker and the diff from v0.127.0 bounded the work.  Both
lockfiles now pin the current template revision, ~llms.org~ carries the
template's rule that a comment justifying a surprise opens with the problem, and
the contributing guide asks for ~just review --diff branch~ before a pull
request.

The judgment half was the review gate again.  The template no longer ships the
binary the Stop hook ran, so keeping the hook, as decided in [[*Finish the compliance sync against rust-template v0.122.0][the v0.122.0 sync]],
meant rebuilding it.  At the operator's direction the hook stays, now a script
over the review tool the template ships, carrying over the old gate's clippy
step and its plan-mode and nested-reviewer exemptions.  ~rust-template.json~
still declares that one deviation.

* DONE Finish the compliance sync against rust-template v0.122.0

The structural checker and the diff from the previously recorded revision
bounded the work.  The pin now names the current template revision in both
lockfiles, which retired the local cross-build source wrapper with it, and
~llms.org~ carries the template's guidance on writing a summary.

The judgment half was the review gate.  The template retires the Claude Code
Stop hook, the script it execs, and the ~template-compliance~ subagent
definition, on the grounds that a review at the end of every turn judges work
that is still being written.  The subagent went, since the gate binary now runs
its own review and nothing else invoked it.  The hook stayed, at the operator's
direction: a review that runs unasked is the one that catches a contradiction
while the change is still in hand, and per-turn batches are what keep each pass
cheap.  ~rust-template.json~ declares that one deviation so the checker reports
it as chosen.  The on-demand reviewer the foundation exposes is wired up beside
it, reached through a ~just~ recipe, for anyone working without that gate.

* TODO Take sqlx 0.9, and the toolchain and hashing bumps it drags along

The database layer sits on sqlx 0.8.  Moving to 0.9 is not a bump but a
migration, and it does not arrive alone.

sqlx 0.9 needs a newer rustc than the flake pins, and the toolchain input has
run well ahead of what this workspace builds with, so taking it is a compiler
upgrade for everything at once.  Against that toolchain the server crate does
not compile: borrowed data escapes the functions that build queries, and 0.9
refuses a dynamically built SQL string without an audit at the call site.  Both
are real signals about the query-building code rather than mechanical breakage.

The ~sha2~ bump belongs to the same move.  On its own it changes what a digest
returns, so the hex formatting stops compiling, and it leaves two copies of the
crate in the tree while sqlx 0.8 holds the older one.  Taking it with sqlx is
what collapses that back to one.

Dependabot has an open pull request for each half; they stay open as the
reminder.  This is a piece of work with its own branch, not something to fold
into a dependency sweep.

* TODO Migrate the frontend off modal dialogs

External review asked for a UI that never hides the encounter queue behind a
modal.  The analysis and migration plan live in [[file:docs/MODAL_MIGRATION.org][docs/MODAL_MIGRATION.org]].  In
progress on the =modal-migration-inline= branch: the Surface rename, the inline
and panel tiers, and the compendium browser's route conversion are implemented.
The popover tier's only planned member, =AbilitySave=, was superseded rather
than converted — clicking an attack roll, ability check, or saving throw now
fires an instant triple-roll (standard + advantage + disadvantage at once) with
no picker UI to anchor, so that tier is empty and closed rather than built.  The
route tier still owes the compendium's edit / paste / group / lore forms plus
=TreasureTable=, and the toast-plus-undo conversions remain.

* DONE Give the Status editor's disabled Apply button a reachable tooltip

Chrome swallows mouse events on a disabled control, so the hover text explaining
why "Apply to Selected" is unavailable never appeared.  Every docked editor's
apply button now routes through =View.Inline.ApplyButton=, which hangs the
tooltip on a wrapper span when the button is unavailable.

* DONE Decide where the workspace's triggers live

Superseded by [[*Fill the editor drawer]]: with every editor in the drawer, the
triggers have no column to live in, because the panels they opened are already
in the drawer, folded to their heading rows.  Only the encounter's own controls
— Compendium, Clear, Reset, Turn — kept a row, at the top of that column.

* DONE Fill the editor drawer

Each surface reaches the drawer now, and the ones still wearing modal chrome
moved in to get there, which is what let the right-hand panes go — and left the
loose ends in [[*Fold the drawer's stragglers into the Surface ADT]].

* DONE Fold the drawer's stragglers into the Surface ADT

Superseded by the drawer becoming a stack: every drawer panel is a ~Surface~
variant in ~model.drawer~ now, and the runtime reconciler is gone with the
one-panel-at-a-time invariant it enforced.

* DONE Drop the cross-build source wrapper once the foundation keeps the source

Each variant helper in ~rust-template~ builds its crane arguments as ~commonArgs
// {src = craneLib.cleanCargoSource self; ...}~, so the source a project passes
is discarded and only Cargo files survive.  This workspace extends that filter
to keep =crates/lib/data/bundled-creatures.json=, which the server crate embeds
with ~include_str!~, so every release binary failed to compile from v0.2.0 until
=flake.nix= put the filtered source back on the finished derivations.

The fix belongs upstream, where the helpers should read ~commonArgs.src or
(craneLib.cleanCargoSource self)~ rather than overwriting it — every downstream
project that filters its source meets the same wall, and =CONTRIBUTING.org= says
these helpers need no per-crate configuration, which the local wrapper makes
untrue.  rust-template carries that shape from v0.121.0, and the ~foundation~
bump to it took the wrapper out.  Its sibling
[[*Drop the cross-build environment wiring once the foundation supplies it]] still
stands: that bump fixed the source and none of the rest.

* TODO Drop the cross-build environment wiring once the foundation supplies it

The variant helpers leave parts of the build environment they need unset, and
=flake.nix= fills those in for them.

~mkMuslPackages~ selects its target with ~CARGO_BUILD_TARGET~ and nothing else,
where its sibling helpers each also name a ~CC~ and ~CXX~ for the target they
build.  A dependency whose build script compiles C is therefore compiled against
the host's glibc and cannot link against musl, which is where the bundled SQLite
that sqlx carries left the static release binaries.  The helper should name them
itself; the glibc-floored variant writes the same pair of zig wrappers a few
lines away, so only the target triple differs.

The portable-glibc helper compiles C through zig without turning zig's
undefined-behaviour checks off, where the macOS helper's compiler wrapper does
turn them off.  Nothing supplies the handlers those checks call, so a proc-macro
library links with them undefined and the compiler cannot load it back.  That
variant builds for the host's own triple, which is why the target's toolchain
reaches a host artifact at all.  =flake.nix= appends the switch through the
flags the ~cc~ crate reads, the helper's own wrapper not being overridable.

The helpers that build through cargo-zigbuild leave crane's dependency check
pass running under an environment their build command does not share, since
cargo-zigbuild supplies its own linker to its own invocation alone.  Cargo reads
that as a reason to re-run every build script, and the bundled SQLite cannot
replace the read-only bindings its first run copied out of the vendor store.
Those helpers already skip the test phase because a cross variant repackages
sources the native build has gated, and the check pass is redundant for the same
reason.

Propose both against rust-template, then delete the local wiring on the
~foundation~ bump that carries them — the same bump that clears
[[*Drop the cross-build source wrapper once the foundation keeps the source]].

* TODO Sweep the "modal" wording out of the migrated editors

Doc comments across the Ui and Update modules still call their surfaces modals,
whether those now edit inline on the card or open as a panel in the editor
column's drawer, so a reader meets a widget the UI no longer has.  The same
drift reaches [[file:docs/ARCHITECTURE.org][docs/ARCHITECTURE.org]], whose model section still names the ADT
~Modal~ and its constructors ~ModalX~.  Retarget them file by file.  Part of
[[*Migrate the frontend off modal dialogs]].

* TODO Tokenise the stylesheet's type scale

CONTRIBUTING.org's "CSS variables for shared values" rule covers every shared
value, but the stylesheet's font sizes and weights are still written as literals
in dozens of rules apiece.  ~--font-size-label~ and ~--font-weight-label~ cover
the group-label shape; define the rest of the scale on ~:root~ and point the
existing rules at it.  Same shape of work as
[[*Sweep style.css to the flat-selector convention]].

* TODO Sweep style.css to the flat-selector convention

The stylesheet predates CONTRIBUTING.org's "Flat selectors only" rule and
carries many descendant and compound selectors outside the sanctioned
theme-override exception.  Replace them with single-class rules (adding view
classes where needed), block by block, without visual regressions.

* DONE Give Save Chain effects durations, failed-save outcomes, and immunities

Three of the settings the preset review asked for.  An effect's duration uses
the Condition editor's picker over a preset-relative form — the target, the
active creature, or a name — that settles at apply time; a repeat save can turn
the condition into another or deal damage again on a failure, from either
editor; and a chain can grant a timed immunity on success that it then honours.
Part of [[*Teach the Save Chain editor the rules its presets need]].

* DONE Give Save Chain effects event-triggered saves, areas, companions, and drain

The rest of the settings the preset review asked for, save one.  A repeat save
can wait on an event rather than a turn boundary: an "ask at end of turn" timing
flashes the chip for the GM to roll when the rule's trigger applied, and a hit
can flash the chip or roll the save outright, with advantage where the rules
grant it.  An area chain marks each target with a chip that rolls the chain
again at the chosen phase of every turn and applies the outcome, with a 🎲 for a
creature entering mid-turn and a × for one leaving.  An effect can name a
companion condition that ends with it, and an outcome can drain — damage that
lowers the hit point maximum by the amount dealt.  Part of
[[*Teach the Save Chain editor the rules its presets need]].

* DONE Teach the Save Chain editor the rules its presets need

The bundled Save Chain presets were checked against the 2024 rules (SRD 5.2.1),
and [[file:docs/SAVE_CHAIN_PRESETS.org][docs/SAVE_CHAIN_PRESETS.org]] records what each can and cannot express.  The
settings the review asked for have landed; the one left open is deliberately a
note: threshold outcomes — a drop to 0 hit points under a ceiling, and automatic
failure for a creature type — cover too few effects to earn a setting, and the
Fail button plus the effect's note carry them.

* DONE Let a condition bring companions, and restore the bundled presets

A condition can apply further conditions that end with it, which the presets use
for conditions that arrive together, and the Load menu can lay the shipped
presets over their stored copies.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Aim a preset's duration at the creature whose turn it is

Effects that last until the caster's or attacker's next turn count the target's
turn instead, since a preset cannot name the source.  A setting that aims the
duration at the active creature when the preset is applied would carry them.
Part of [[*Teach the Condition/Effect editor the rules its presets need]].

* TODO End a condition on damage without a save

Some effects end the moment the bearer takes damage, which the save's damage
settings can only flag or roll.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Deal a condition's damage on the bearer's turn

Some effects deal damage every turn whatever the save does, where the
failed-save damage fires only on a failure.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Offer an escape check beside the save to end

Restraining effects and grapples end on a Strength (Athletics) check, which the
presets stand in for with a manual Strength save.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Let a countdown run past 99 turns, or roll its length

Ten-minute, hour-long, and rolled durations last until removed.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO End a spell effect with its caster's concentration

Most spell presets end when the caster's concentration does, which the GM tracks
by hand; the companion link would have to reach across creatures to the caster's
Concentrating status.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Grant temporary hit points from a condition

A few effects grant temporary hit points, once or on each of the bearer's turns,
which the HP editor sets only by hand.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Add Exhaustion on a schedule

Suffocation and extreme weather add a level of Exhaustion on a schedule without
ending.  Part of [[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Grant immunity after a successful save

A few monster effects leave a creature that saves immune for a day, which the
Save Chain editor already grants.  Part of
[[*Teach the Condition/Effect editor the rules its presets need]].

* TODO Teach the Condition/Effect editor the rules its presets need

The bundled Condition/Effect presets were checked against the 2024 rules (SRD
5.2.1 where the effect is in it), and [[file:docs/CONDITION_PRESETS.org][docs/CONDITION_PRESETS.org]] records where
each falls short and what supporting it would take.  The entries that name this
one carry the simple and moderate settings it asks for; the complex gaps stay
with the GM, as that document records, until the tracker models those mechanics.
